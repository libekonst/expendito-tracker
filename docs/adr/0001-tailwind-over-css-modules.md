# Tailwind over CSS Modules

The PRD specifies CSS Modules as the styling strategy. The project scaffold was set up with Tailwind v4 instead, and all prototype work was done in Tailwind. Migrating back to CSS Modules would require rewriting every component; the cost is not worth it for a single-user tool with no existing CSS Modules code. Tailwind stays.
